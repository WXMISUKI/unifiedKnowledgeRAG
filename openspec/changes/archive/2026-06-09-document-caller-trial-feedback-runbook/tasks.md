## 1. Specification

- [x] 1.1 Create the `document-caller-trial-feedback-runbook` OpenSpec proposal, design, and tasks for the post-closure caller trial feedback workflow.

## 2. Documentation

- [x] 2.1 Add a caller trial feedback runbook that sequences Phase 15 dispatch, Phase 16 access loop, trial outcome export, and Phase 25 provider feedback consumption.
- [x] 2.2 Update the next-stage roadmap note to explicitly split the post-closure work into ordered stages.
- [x] 2.3 Update the provider progress tracker to record the runbook as the current execution entrypoint after provider closure.

## 3. Validation And Archive

- [x] 3.1 Run `openspec validate --all --strict`.
- [x] 3.2 Archive the change.
