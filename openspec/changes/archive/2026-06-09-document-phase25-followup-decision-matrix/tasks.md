## 1. Specification

- [x] 1.1 Create the `document-phase25-followup-decision-matrix` OpenSpec proposal, design, and tasks for the Phase 25 follow-up decision matrix.

## 2. Documentation

- [x] 2.1 Add a Phase 25 follow-up decision matrix that maps provider feedback results to default next actions.
- [x] 2.2 Update the next-stage roadmap note so stage 3 is explicitly represented as a decision matrix rather than an implicit discussion step.
- [x] 2.3 Update the provider progress tracker to record the matrix as the stage-3 decision entrypoint.

## 3. Validation And Archive

- [x] 3.1 Run `openspec validate --all --strict`.
- [x] 3.2 Archive the change.
