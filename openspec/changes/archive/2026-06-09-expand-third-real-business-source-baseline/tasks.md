## 1. Specification

- [x] 1.1 Create the `expand-third-real-business-source-baseline` OpenSpec proposal, design, and delta spec with roadmap-aligned scope and non-goals.

## 2. Implementation

- [x] 2.1 Extend the aggregate real-business fixture with `logistics_faq` answerable and negative-control cases.
- [x] 2.2 Update focused aggregate tests so the third source is included in the happy path and missing-source path.

## 3. Validation And Evidence Refresh

- [x] 3.1 Run focused pytest coverage for aggregate business golden cases and related local baseline tests.
- [x] 3.2 Run `openspec validate --all --strict`, refresh the aggregate real-business artifacts, and update the provider improvement tracker.
