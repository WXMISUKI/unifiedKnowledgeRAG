## 1. Specification

- [x] 1.1 Create the `document-provider-next-step-trigger-contract` OpenSpec proposal, design, and delta spec with explicit pause-state and trigger-class rules.

## 2. Documentation Update

- [x] 2.1 Update roadmap notes to state that provider-side expansion is paused by default until a stronger trigger appears.
- [x] 2.2 Update the RAG techniques adoption note to shift the next-step recommendation from feature expansion to trigger-based reopening rules.
- [x] 2.3 Update the provider progress tracker with the new trigger contract and paused-expansion posture.

## 3. Validation And Archive

- [x] 3.1 Run `openspec validate --all --strict` and verify the new trigger contract is reflected consistently in roadmap/progress docs.
- [x] 3.2 Archive the change after the documentation-only contract is complete.
