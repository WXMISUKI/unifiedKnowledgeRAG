## 1. Specification

- [x] 1.1 Create the `document-caller-trial-outcome-input-contract` OpenSpec proposal, design, tasks, and spec deltas for the caller-side live trial outcome input contract.

## 2. Implementation

- [x] 2.1 Add a caller-facing trial outcome input contract document that explains required fields, optional fields, boundaries, and fail-closed expectations.
- [x] 2.2 Add a reusable example JSON file for caller-side live trial outcome export.
- [x] 2.3 Update the Phase 25 feedback service to make missing critical fields visible through conservative evidence warnings/blockers.
- [x] 2.4 Add focused tests for missing or partial critical-field input behavior.

## 3. Documentation Sync

- [x] 3.1 Update roadmap notes to state that the next practical priority after provider closure is stabilizing real caller feedback input, not adding new provider features.
- [x] 3.2 Update the progress tracker to record the new caller-trial-outcome-input contract slice and its role in the post-closure workstream.

## 4. Validation And Archive

- [x] 4.1 Run focused tests for Phase 25 feedback parsing and strict OpenSpec validation.
- [x] 4.2 Archive the change after the contract, example, implementation, and docs are complete.
