## 1. Specification

- [x] 1.1 Create the `negative-control-hardening` OpenSpec proposal, design, and delta spec with the roadmap-aligned scope and non-goals.

## 2. Implementation

- [x] 2.1 Add minimal weak-overlap suppression to the fixture document retriever while preserving exact-term refund-policy lookups.
- [x] 2.2 Add focused tests for the negative-control leakage case and for positive-control stability.

## 3. Validation And Evidence Refresh

- [x] 3.1 Run focused pytest coverage for local and aggregate business golden cases plus the new retriever test.
- [x] 3.2 Run `openspec validate --all --strict`, refresh the real-business aggregate artifacts, and update the provider improvement tracker.
