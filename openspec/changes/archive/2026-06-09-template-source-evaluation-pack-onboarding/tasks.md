## 1. Specification

- [x] 1.1 Create the `template-source-evaluation-pack-onboarding` OpenSpec proposal, design, and delta spec with explicit provider-level scope and non-goals.

## 2. Implementation

- [x] 2.1 Add a source evaluation pack onboarding helper that generates baseline, failed-question, and confirmation fixture templates plus an onboarding manifest/report for a given source.
- [x] 2.2 Add focused tests that verify the helper creates the expected files, field skeletons, and next-step guidance conservatively.

## 3. Validation And Evidence Refresh

- [x] 3.1 Run focused pytest coverage for the onboarding helper and related evaluation-pack tests.
- [x] 3.2 Run `openspec validate --all --strict`, refresh onboarding template artifacts, and update roadmap/progress documentation.
